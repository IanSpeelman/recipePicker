<%- layout("./layout/boilerplate") %> <% recipes.forEach(recipe => { %>
<div>
	<a href="/recipes/<%= recipe._id %>">
		<h1> <%= recipe.name %> </h1>
		<h2><%= recipe.description %></h2>
	</a>
</div>
<% }) %>
